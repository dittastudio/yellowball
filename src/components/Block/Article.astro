<!-- Begin Block: Article -->
<div class="wrapper grid gap-x-30 gap-y-80 grid-cols-8 lg:grid-cols-12">
  {
    Astro.slots.content && (
      <div class="lg:order-2 col-span-full lg:col-start-5 lg:col-span-8">
        <slot name="content" />
      </div>
    )
  }

  {
    Astro.slots.aside && (
      <aside class="lg:order-1 flex flex-col justify-start items-start gap-20 col-span-full lg:col-start-1 lg:col-span-3">
        <slot name="aside" />
      </aside>
    )
  }
</div>
<!-- End Block: Article -->
