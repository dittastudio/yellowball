---
import type { HTMLAttributes } from 'astro/types';

interface Base {
  name: string;
}

type Props = HTMLAttributes<'svg'> & Base;

const { name, ...rest } = Astro.props;
---

<svg xmlns="http://www.w3.org/2000/svg" {...rest}>
  <use href={`/sprite.svg#${name}`}></use>
</svg>
