---
import { twMerge } from "tailwind-merge";
import Icon from "@/components/Elements/Icon.astro";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props as Props;

// TODO: Fix the merge issue with conif in TW? "text-12" gets removed.
const classes = twMerge(
  "font-bold text-12 text-navy rounded-lg px-10 py-5 flex justify-center items-center gap-10 select-none bg-green",
  className
);
---

<p class:list={[classes]}>
  <Icon name="cursor" />
  {title}
</p>
